# Password Generator (CLI)

## Description
A Python program that generates random passwords.

It allows the user to create strong passwords based on specific requirements such as length and character types.

## Features
- Generate passwords of custom length
- Include/exclude:
  - Uppercase letters
  - Lowercase letters
  - Digits
  - Symbols

## Usage
Run the program using:

```bash
python generator.py